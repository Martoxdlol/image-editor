# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# The wasm core (src/wasm-pkg/) is prebuilt and committed so the Cloudflare
# Pages build does not need a Rust toolchain. See src/wasm-pkg/.gitignore for
# the exact tracked artifacts. Build locally with `pnpm wasm`.
